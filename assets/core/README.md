# Core asset bundles baked into the build (AssetManager priority=core).
# Each <id>/ holds meta.json + atlas PNG(s). Drop real art here as it lands;
# until then the game renders the labeled PixelSlot placeholders.
